// stripping names:

// with whitespace
let person_name = "\talishba khan\n";

console.log("name with whitespace");
console.log(person_name)

//without whitespace
let strippedName = person_name.trim();

console.log("Name after stripping whitespace:");
console.log(strippedName);

